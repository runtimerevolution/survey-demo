<div class="container">
  <h1><%= @attempt.survey.name %></h1>
  <% @attempt.answers.each do |answer| %>
    <p><%= answer.question.text %></p>
    <% answer.question.options.each do |option| %>
      <div class="radio">
        <label>
          <%= radio_button_tag '', '', the_chosen_one?(answer, option), disabled: true %>
          <%= option.text %>
          <% if option.correct %>
            <b> <%= number_of_people_who_also_answered(option.id) %> <b>
          <% else %>
            <%= number_of_people_who_also_answered(option.id) %>
          <% end %>
        </label>
      </div>
    <% end %>
  <% end %>
</div>